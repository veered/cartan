#!/usr/bin/env ruby
$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'

require 'cartan'
require 'thor'

class CartanResource < Thor

	desc "start", "Start the resource."
	method_option :config, :type => :string, :aliases => "-c",
				  :desc => "The path of the config file."
	def start
		orchestrator = Cartan::Resource.new options[:config]
	end

end

CartanResource.start